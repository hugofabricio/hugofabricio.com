import * as tokens from './tokens'

const theme = {
  awesomegrid: tokens.grid,
  breakpoints: tokens.breakpoints,
  typography: tokens.typography,
  colors: tokens.colors,
  button: tokens.button,
  form: tokens.form
}

export default theme
